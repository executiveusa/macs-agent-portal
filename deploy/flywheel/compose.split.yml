services:
  maxx-control-plane:
    ports:
      - "${MAXX_CONTROL_PLANE_BIND:-127.0.0.1:8788}:8787"
    networks:
      - default
      - supabase_net

  maxx-second-brain-worker:
    networks:
      - default
      - supabase_net

networks:
  default:
  supabase_net:
    name: supabase_default
    external: true

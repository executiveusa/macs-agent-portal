services:
  maxx-control-plane:
    ports:
      - "${MAXX_CONTROL_PLANE_BIND:-127.0.0.1:8788}:8787"
    networks:
      - default
      - supabase_default

  maxx-second-brain-worker:
    networks:
      - default
      - supabase_default

networks:
  supabase_default:
    external: true
